# Demo defaults work out of the box; copy to backend/.env and fill for real deploys.
# SQLite demo (default when unset): sqlite:///./parking_demo.db
DATABASE_URL=sqlite:///./parking_demo.db
# REQUIRED in production: long random string. Never commit the real one.
JWT_SECRET=
# 8h guard shift
JWT_EXPIRE_MINUTES=480
