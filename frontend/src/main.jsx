import React from 'react'
import ReactDOM from 'react-dom/client'
import { BrowserRouter } from 'react-router-dom'
import App from './App.jsx'
import './index.css'

// Root entry — wraps the entire SPA in React Router's BrowserRouter so all
// route components can leverage <Link>, <Navigate>, and useNavigate() without
// importing the Router themselves.
ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <BrowserRouter>
      <App />
    </BrowserRouter>
  </React.StrictMode>,
)
